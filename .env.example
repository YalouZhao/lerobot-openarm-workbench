# Optional environment overrides for local deployment.
# Do not put real tokens or private keys in git.

WORKBENCH_HOST=0.0.0.0
WORKBENCH_PORT=8090
CONDA_SH=/home/robot/miniconda3/etc/profile.d/conda.sh
CONDA_ENV=lerobot
